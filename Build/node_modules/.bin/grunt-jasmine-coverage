#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../grunt-jasmine-coverage/bin/grunt-jasmine-coverage" "$@"
  ret=$?
else 
  node  "$basedir/../grunt-jasmine-coverage/bin/grunt-jasmine-coverage" "$@"
  ret=$?
fi
exit $ret
